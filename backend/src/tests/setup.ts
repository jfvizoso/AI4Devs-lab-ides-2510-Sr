// Setup global para tests
// Este archivo puede ser usado para configuración global de tests

beforeAll(async () => {
  // Configuración antes de todos los tests
  // Por ejemplo, conectar a base de datos de test
});

afterAll(async () => {
  // Limpieza después de todos los tests
  // Por ejemplo, cerrar conexiones
});

